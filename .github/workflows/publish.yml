name: publish site
on:
  push:
    branches:
      - main  # あなたのメインブランチ名に合わせてください

permissions:
  contents: write

jobs:
  deploy:
    # デプロイ環境
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          # Pythonのバージョンを指定
          python-version: 3.x
      - run: | # MkDocsと必要なプラグインをインストール
          pip install mkdocs-material
          pip install mkdocs-table-reader-plugin

      # MkDocsを使用してサイトをビルドし、GitHub Pagesにデプロイ
      - run: mkdocs gh-deploy --force

      # Discord Webhookを使用してデプロイ完了を通知
      - name: Discord Notification
        if: always() # デプロイが成功してもしなくても通知を送る
        env:
          WEBHOOK_URL: ${{ secrets.DISCORD_WEBHOOK_URL }} # GitHub Secretsに設定したWebhook URLを使用
          STATUS: ${{ job.status }} # ジョブのステータスを取得
          SITE_URL: "https://firesepichub-14.github.io"
          RUN_URL: "https://github.com/${{ github.repository }}/actions/runs/${{ github.run_id }}"
        run: |
          # 16進数カラーコードを10進数に変換(#抜きで)
          # HEX_SUCCESS="39C5BB" # ミク色  --> 3786171
          # HEX_FAILURE="e25577" # テト色  --> 14832503

          # 基本情報を定義
          COMMIT= "コミット: ${{ github.event.head_commit.message }}"
          BRANCH= "ブランチ: ${{ github.ref }}"
          REPO= "リポジトリ: ${{ github.repository }}"
          ACTOR= "実行者: ${{ github.actor }}"

          # デプロイのステータスに応じたメッセージを作成
          if [ "$STATUS" == "success" ]; then
            TITLE="🚀 MkDocs デプロイ成功！"
            COLOR=3786171
            DESC_MSG_L1="✅ サイトが正常にデプロイされました！"
            DESC_MSG_L2="　　サイトが最新の状態に更新されました！"
            CONTENT=""
          else
            TITLE="❌ MkDocs デプロイ失敗..."
            COLOR=14832503
            DESC_MSG_L1="⚠️ デプロイに失敗しました。"
            DEsC_MSG_L2="　　詳細は以下のボタンから確認してください。"
            CONTENT="<@791925284009017344>" s
          fi

          # 組み立て
          FULL_DESC="${COMMIT}\n${BRANCH}\n${REPO}\n${ACOTR}\n\n${DESC_MSG_L1}\n${DESC_MSG_L2}"

          # JSONからDiscord Webhookのペイロードを作成
          PAYLOAD=$(jq -n \
            --arg title "$TITLE" \
            --arg site_url "$SITE_URL" \
            --arg run_url "$RUN_URL" \
            --arg desc "$FULL_DESC" \
            --arg content "$CONTENT" \
            --argjson color $COLOR \
            '{
              "content": $content,
              "embeds": [
                {
                  "title": $title,
                  "description": $desc,
                  "color": $color | tonumber,
                  ],
                  components: [
                    {
                      "type": 1,
                      "components": [
                        {
                          "type": 2,
                          "label": "サイトを確認",
                          "style": 5,
                          "url": $site_url
                        },
                        {
                          "type": 2,
                          "label": "GitHub Actionsを確認",
                          "style": 5,
                          "url": $run_url
                        }
                      ]
                    }
                  ]
                }
              ]
            }')
          
          curl -H "Content-Type: application/json" -X POST -d "$PAYLOAD" "$WEBHOOK_URL"