name: publish site
on:
  push:
    branches:
      - main  # あなたのメインブランチ名に合わせてください

permissions:
  contents: write

jobs:
  deploy:
    # デプロイ環境
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          # Pythonのバージョンを指定
          python-version: 3.x
      - run: | # MkDocsと必要なプラグインをインストール
          pip install mkdocs-material
          pip install mkdocs-table-reader-plugin

      # MkDocsを使用してサイトをビルドし、GitHub Pagesにデプロイ
      - run: mkdocs gh-deploy --force

      # Discord Webhookを使用してデプロイ完了を通知
      - name: Discord Notification
        uses: sarisia/actions-status-discord@v1
        if: always()  # デプロイが失敗しても通知を送る
        with:
          webhook: ${{ secrets.DISCORD_WEBHOOK_URL }}
          status: ${{ job.status }}
          # デプロイの成功/失敗に応じたタイトルを設定
          title: "${{ job.status == 'success' && '🚀 MkDocs デプロイ成功！' || '⚠️ デプロイ失敗...'}}"

          description: |
            コミット: ${{ github.event.head_commit.message }}
            ブランチ: ${{ github.ref }}
            リポジトリ: ${{ github.repository }}
            ${{ job.status == 'success'
              && format('✅ サイトが正常にデプロイされました！ \n サイトが最新の状態に更新されました！')
              || format('❌デプロイに失敗しました... \nログを確認してください。: https://github.com/{0}/actions/runs/{1}', github.repository, github.run_id)
            }}

          color_success: "#39C5BB"
          color_failure: "#e25577"
          color_cancelled: "#ffe211"
          image: "https://firesepichub-14.github.io"